/*import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule } from '@angular/forms'; // Para usar formularios reactivos
import { RouterModule } from '@angular/router'; // Para enrutamiento
import { MaterialModule } from '../../material.module'; // Asegúrate de que la ruta sea correcta

// Importa tu componente
import { AppSideLoginComponent } from './side-login/side-login.component';

@NgModule({
  declarations: [
    AppSideLoginComponent,  // Aquí declaras el componente
    // otros componentes de autenticación como AppSideRegisterComponent
  ],
  imports: [
    CommonModule,
    MaterialModule,
    ReactiveFormsModule,  // Asegúrate de importar ReactiveFormsModule
    RouterModule,         // Asegúrate de importar RouterModule si se usa enrutamiento
  ],
  exports: [
    AppSideLoginComponent,  // Exporta el componente si lo necesitas en otros módulos
  ],
})
export class AuthenticationModule {}
*/